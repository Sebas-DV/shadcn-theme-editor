import "./globals.css";

// Server-rendered stacks (Laravel + Inertia, SvelteKit, Astro) would add this
// single line instead of relying on index.html injection:
//   import "virtual:shadcn-theme-editor";
